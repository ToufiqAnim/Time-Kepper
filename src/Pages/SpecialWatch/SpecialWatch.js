import React from 'react';
import { Carousel, Container } from 'react-bootstrap';

const SpecialWatch = ({specialWatch}) => {
    const{name, image, price, details} = specialWatch;
    return (
       <Container>
           
       </Container>
    );
};

export default SpecialWatch;